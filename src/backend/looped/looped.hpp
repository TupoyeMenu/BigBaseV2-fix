/**
 * @file looped.hpp
 * @brief Looped features declarations.
 * To add a new looped feature make a file in `looped` folder,
 * make a function and declare it hare, then add it to `backend.cpp`.
 */

#pragma once

namespace big
{
	class looped
	{
	public:
		static void system_self_globals();
	};
}
